#include <stdio.h>
#include <math.h>
main(){
	int num,result;
	result=num*num;
	printf("Enter a Number:");
	scanf("%d", &num);
	printf("The Square of The Entered Number is = %d\n", result);
}
